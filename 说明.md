# 说明
这是一个 Revit 插件项目，当前提供 **2022** 与 **2024** 两套构建。GitHub Actions 制品为 zip，命名示例：`speckle-upload-2024-20260527100401-a1b2c3.zip`（Revit 版本 + 构建时间 + 6 位 commit）。  
## 插件主要功能
- 插件会启动一个http监听，默认端口是6688，可以通过环境变量 SPECKLE_UPLOAD_HTTP_PORT 配置，如果没配置就用默认的
- 插件会向本机 speckle_sync 服务发送完成回调，默认 `http://127.0.0.1:6689/api/callback`；可通过上传请求里的 `callbackUrl` 或环境变量 `SPECKLE_UPLOAD_CALLBACK_URL` 覆盖。对接细节见 [SPECKLE_SYNC.md](./SPECKLE_SYNC.md)
- revit启动的时候，自动启动这个插件（就是自动运行这个http）
- 用较新版本 Revit 打开旧版 RVT 时会触发升级与后续提示框。插件目录下 **`SpeckleUpload.open-dialog-rules.json`** 按规则代点：`never` 列表（含「取消升级」）**永不代点**；`rules` 按文案匹配后点 `close`/`ok` 等。时长见 `SPECKLE_UPLOAD_OPEN_DIALOG_SUPPRESS_SECONDS`（默认 60 秒）
- 我会通过http发送指令，需要实现以下功能:
  1. 打开指定的文件。若已有其它打开的文档：先打开接口指定的文件（使其成为活动文档），再关闭其余非链接文档（Revit API 不允许先 API 关闭当前活动文档）；若当前活动文档已是目标路径则只关闭其它文档。
  2. 使用 Speckle.ConnectorRevit.ConverterRevit 初始化转换器。
  3. 获取模型中所有的 Physical Objects
  4. 使用 Speckle.Core.Api.Operations.Send 将转换后的对象发送到指定的 streamId。
  5. 发送完成后，通过 HTTP 回传结果给 callback 的地址（JSON 字段为 snake_case：`request_id`、`file_path`、`stream_id`、`branch_name`、`commit_message`、`object_id`、`commit_id`、`object_count`、`success`、`error`），然后关闭当前文档。

然后还需要帮我生成下.addin文件，这是Revit软件识别插件的关键:
```
帮我生成一个 SpeckleUpload.addin 文件。
ClientId 随机生成一个 GUID。
FullClassName 指向我的 SpeckleUploadApp。
Name 为 SpeckleUpload。
运行范围是 Revit 2022。
```

# 安装步骤
1. 先去github上找到编译好的压缩包，在runs页面(最下面 Artifacts下方)
2. 下载后，进入revit的插件目录，将.addin文件放进去，我这里是直接将.addin和解压后的文件夹一起放进去
3. 打开 .addin 文件，修改Assembly属性，指定dll文件，比如我这里修改成:
```
<Assembly>C:\Users\admin\AppData\Roaming\Autodesk\Revit\Addins\2022\SpeckleUpload\SpeckleUpload.dll</Assembly>
```
4. 打开revit，这时候会提示加载我新加的插件，刚开始有不受信任的提示，点一直加载或者加载一次就可以
5. 这时候会报错，报错内容是外部工具什么的，这时候按照ai的建议，点击某个.dll文件，然后右键属性，点击最下方解锁，或者powershell执行命令，可以把整个文件夹的.dll都解锁（推测是windows的防病毒机制）:
```
$dir = "C:\Users\admin\AppData\Roaming\Autodesk\Revit\Addins\2022\SpeckleUpload"
Get-ChildItem -Path $dir -Recurse -File | Unblock-File
```
6. 再次打开，就正常了
7. 验证(浏览器):
```
http://localhost:6688/health
```

## 自动部署脚本（Windows）

仓库提供 **`Install-SpeckleUpload.cmd`**（Revit **2022**）与 **`Install-SpeckleUpload-2024.cmd`**（Revit **2024**），以及 **`Install-SpeckleUpload.ps1`**（可用 `-RevitYear 2022|2024`）。

**推荐：** 解压对应版本的 zip 后，双击对应 `.cmd`。它会用 `-ExecutionPolicy Bypass` 调用 `.ps1`，避免执行策略报错。

若仍想直接运行 `.ps1`，可在 **当前用户** 放宽策略（管理员不必）：

```powershell
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned
```

或单次绕过（不改系统策略）：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File ".\Install-SpeckleUpload.ps1"
```

脚本会：

1. 以**脚本所在目录**为源目录（绝对路径；`.cmd` 启动时工作目录为解压目录）
2. 清空并重建 `%APPDATA%\Autodesk\Revit\Addins\2022\SpeckleUpload`（与 `C:\Users\<用户名>\AppData\Roaming\...` 等价）
3. 将源目录下**除 `Install-SpeckleUpload.ps1` 以外**的全部文件和文件夹**移动**到上述插件目录（`.cmd` 不会被移动）
4. 对插件目录递归执行 `Unblock-File`（与下方手动命令等价）
5. 提示「版本替换完成」后 **`pause`**（按任意键关闭窗口）

首次安装仍需将 `SpeckleUpload.addin` 放到 `%APPDATA%\Autodesk\Revit\Addins\2022\` 并修改 `<Assembly>` 指向插件目录下的 `SpeckleUpload.dll`（见上文「安装步骤」）；之后升级只需重新下载 zip、解压并运行 **`Install-SpeckleUpload.cmd`** 即可。

手动解锁（与脚本第 4 步一致）：

```
$dir = "C:\Users\admin\AppData\Roaming\Autodesk\Revit\Addins\2022\SpeckleUpload"
Get-ChildItem -Path $dir -Recurse -File | Unblock-File
```

## 本地编译（可选）

```bash
# Revit 2022（默认）
dotnet build SpeckleUpload.sln -c Release

# Revit 2024
dotnet build SpeckleUpload.sln -c Release -p:RevitVersion=2024
```

输出目录分别为 `bin/2022/Release/net48`、`bin/2024/Release/net48`。`.addin` 放入 `%APPDATA%\Autodesk\Revit\Addins\{2022|2024}\`，`<Assembly>` 指向对应目录下的 `SpeckleUpload.dll`。

## 快速替换步骤

```
1. 下载 Actions 里对应版本的 zip（文件名含 Revit 版本、时间、commit，如 `speckle-upload-2024-20260527100401-a1b2c3.zip`）并解压。
2. 双击 Install-SpeckleUpload.cmd（2022）或 Install-SpeckleUpload-2024.cmd（2024）。
3. 将 SpeckleUpload.addin 放到 %APPDATA%\Autodesk\Revit\Addins\{2022|2024}\ 并设置 Assembly 路径（首次安装）。
4. 完全退出 Revit 后重新打开以加载新版本。
```